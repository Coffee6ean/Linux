const config = {
    API_BASE_URL: 'http://localhost:5000/api',
    appVersion: '1.0.0',
    theme: 'light',
    featureFlags: {
        newDashboard: true,
        analyticsEnabled: false
    }
};

export default config;